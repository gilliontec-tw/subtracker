---
phase: 04-ux-renewal
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/api/subscriptions.ts
  - frontend/src/components/subscriptions/SubscriptionTable.tsx
  - frontend/src/components/subscriptions/BatchRenewDialog.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Each subscription row has a leading checkbox and the header has a select-all checkbox"
    - "Each row's action column shows a 錢袋 icon (lucide Wallet) that opens PaymentRecordFormDialog pre-filled with that subscription_id; the trash icon is removed"
    - "When >=1 row is checked, a 續訂 button appears (near the search/header area above the table); when none checked it is hidden"
    - "Clicking 續訂 opens BatchRenewDialog listing each selected subscription with 舊到期日 → 新到期日, and warnings for ineligible (cancelled/suspended/missing billing_cycle) — ineligible rows are visibly flagged and excluded from the actual call"
    - "Confirming the dialog calls POST /api/v1/subscriptions/batch-renew, on success invalidates ['subscriptions'] query, clears selectedIds, and shows a toast summarizing renewed N / skipped M"
  artifacts:
    - path: frontend/src/components/subscriptions/BatchRenewDialog.tsx
      provides: "Confirmation dialog listing date deltas and warnings"
    - path: frontend/src/api/subscriptions.ts
      provides: "batchRenewSubscriptions(ids) API function"
  key_links:
    - from: frontend/src/components/subscriptions/SubscriptionTable.tsx
      to: BatchRenewDialog
      via: "selectedIds state + 續訂 button"
      pattern: "BatchRenewDialog"
    - from: frontend/src/api/subscriptions.ts
      to: "/api/v1/subscriptions/batch-renew"
      via: "axios POST"
      pattern: "batch-renew"
---

<objective>
重設計訂閱列表操作流程：加入勾選欄、批量續訂入口、錢袋付款 icon，並移除列表行的垃圾桶。

Purpose: 實作 D-01, D-05, D-06, D-07, D-08, D-10, D-11, D-13, D-14。
Output: 更新 SubscriptionTable、新增 BatchRenewDialog、擴充 api/subscriptions.ts。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-ux-renewal/04-CONTEXT.md
@frontend/src/components/subscriptions/SubscriptionTable.tsx
@frontend/src/components/payments/PaymentRecordFormDialog.tsx
@frontend/src/api/subscriptions.ts

<interfaces>
From `frontend/src/types/api.ts` (existing):
- `Subscription` includes `id: number`, `service_name: string`, `expiry_date: string` (ISO date), `billing_cycle: string | null`, `status: string`.

From `frontend/src/components/payments/PaymentRecordFormDialog.tsx`:
- Props: `{ open, onOpenChange, subscriptionId?: number, record?: PaymentRecord }`
- Without `record`, dialog acts as "create" form using `subscriptionId`.

From `frontend/src/api/client.ts`:
- `api` (axios) auto-injects CSRF token, withCredentials.

From `frontend/src/hooks/use-toast.ts`:
- `useToast()` returns `{ toast }`. Call `toast({ title, variant })`.

Backend endpoint (from Plan 04-01):
- POST `/api/v1/subscriptions/batch-renew`
- Body: `{ "subscription_ids": number[] }`
- Response data: `{ renewed: Subscription[], skipped: { id: number, reason: "not_found" | "not_active" | "missing_billing_cycle" }[] }`

No shadcn `Checkbox` component exists in the project — use native `<input type="checkbox" class="size-4">` (same pattern as `SubscriptionForm.tsx` `auto_renew` field).

Cycle label map (already defined in SubscriptionForm.tsx / SubscriptionDetailDialog.tsx):
- monthly→每月, quarterly→每季, semi_annual→每半年, annual→每年, biennial→每兩年
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 batchRenewSubscriptions API function</name>
  <files>frontend/src/api/subscriptions.ts</files>
  <read_first>
    - frontend/src/api/subscriptions.ts
    - frontend/src/api/client.ts
  </read_first>
  <action>
    Append to `frontend/src/api/subscriptions.ts`:

    Export type `BatchRenewSkipped = { id: number; reason: 'not_found' | 'not_active' | 'missing_billing_cycle' }` and `BatchRenewResult = { renewed: Subscription[]; skipped: BatchRenewSkipped[] }`.

    Export `async function batchRenewSubscriptions(ids: number[]): Promise<BatchRenewResult>` that POSTs to `/api/v1/subscriptions/batch-renew` with body `{ subscription_ids: ids }`, validates `data.success && data.data`, returns `data.data`.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>Function exported with correct types; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: 建立 BatchRenewDialog 元件</name>
  <files>frontend/src/components/subscriptions/BatchRenewDialog.tsx</files>
  <read_first>
    - frontend/src/components/subscriptions/DeleteConfirmDialog.tsx (mutation + invalidate pattern)
    - frontend/src/components/payments/PaymentRecordFormDialog.tsx (Dialog structure)
    - frontend/src/api/subscriptions.ts (after Task 1)
  </read_first>
  <action>
    Create `BatchRenewDialog` component with props `{ subscriptions: Subscription[]; open: boolean; onOpenChange: (open: boolean) => void; onSuccess?: () => void }`.

    Compute, for each input subscription, an `eligible` flag and `newExpiry` string:
    - ineligible if `status !== 'active'` → reason 「狀態非啟用中，本次將略過」
    - ineligible if `billing_cycle == null` → reason 「缺少計費週期，請先編輯訂閱」
    - else compute `newExpiry` from `expiry_date` + cycle using a local helper `addCycle(dateStr, cycle)` (monthly=+1mo, quarterly=+3mo, semi_annual=+6mo, annual=+1yr, biennial=+2yr; clamp Feb-29 to Feb-28 when needed; produce `YYYY-MM-DD`).

    Render a `<Dialog>` with `DialogHeader` title 「確認批量續訂」, body = a vertical list (or `<Table>`) of one row per subscription showing: `service_name`、`billing_cycle` 中文標籤、`expiry_date` → `newExpiry`（ineligible rows show warning message in destructive color instead of new date）。底部顯示總結「將續訂 X 筆，略過 Y 筆」。

    Footer: 取消 button + 確認續訂 button (`variant="destructive"` 不適合，用預設 primary)。

    On confirm, use `useMutation` calling `batchRenewSubscriptions(eligibleIds)` where `eligibleIds = subscriptions.filter(eligible).map(s => s.id)`. If `eligibleIds.length === 0`, disable the confirm button.

    On success:
    - `queryClient.invalidateQueries({ queryKey: ['subscriptions'] })`
    - `toast({ title: \`已續訂 ${result.renewed.length} 筆，略過 ${result.skipped.length} 筆\` })`
    - call `onSuccess?.()` then `onOpenChange(false)`

    On error: `toast({ title: '批量續訂失敗', variant: 'destructive' })`.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>Component renders, eligibility logic correct, mutation wired, tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: 改造 SubscriptionTable - checkbox + 續訂 button + 錢袋 icon + 移除垃圾桶</name>
  <files>frontend/src/components/subscriptions/SubscriptionTable.tsx</files>
  <read_first>
    - frontend/src/components/subscriptions/SubscriptionTable.tsx
    - frontend/src/components/subscriptions/BatchRenewDialog.tsx (from Task 2)
    - frontend/src/components/payments/PaymentRecordFormDialog.tsx
  </read_first>
  <action>
    Modify `SubscriptionTable.tsx`:

    1. Add `selectedIds` state: `const [selectedIds, setSelectedIds] = useState<Set<number>>(new Set())`.
    2. Add `paymentSubId` state: `const [paymentSubId, setPaymentSubId] = useState<number | null>(null)`.
    3. Add `batchOpen` state: `const [batchOpen, setBatchOpen] = useState(false)`.
    4. Import `Wallet` from `lucide-react` (D-01 — use `Wallet`); remove `DeleteConfirmDialog` import and remove the `canDelete` row-level usage (keep variable if still needed for `hasActions`).
    5. Update `hasActions` to `canUpdate` only (since trash removed; wallet shown when canCreate; combine: `const hasActions = canUpdate || canCreate`). Pull `canCreate = currentUser?.can_create || currentUser?.role === 'admin'`.
    6. Prepend a new leading `<TableHead>` containing the header checkbox. Header checkbox is `checked` when `selectedIds.size === sorted.length && sorted.length > 0`, `indeterminate` when 0 < size < length (set via `ref` callback: `el.indeterminate = ...`). onChange toggles all sorted ids.
    7. Prepend a new leading `<TableCell>` in each row containing a checkbox bound to `selectedIds.has(sub.id)`; onChange toggles that id in the set. Wrap in `onClick={(e) => e.stopPropagation()}` div so it does not open detail dialog.
    8. Above the table, render a header row (new wrapper `<div className="mb-3 flex items-center justify-end">`) containing the 續訂 button — only when `selectedIds.size > 0`. Button label: `續訂 (${selectedIds.size})`. onClick → `setBatchOpen(true)`. Per D-08, button position is "搜尋欄右側" — render in this component above the table; the parent page already owns search UI, so the button living above the table is the table's responsibility.
    9. In the actions cell: keep 鉛筆 button. Replace the `<DeleteConfirmDialog>` element with a wallet button: `<Button variant="ghost" size="icon" onClick={() => setPaymentSubId(sub.id)}><Wallet className="size-4" /></Button>`. Only render wallet when `canCreate`.
    10. Update the empty-row `colSpan` to account for the new leading checkbox column (`hasActions ? 9 : 8`).
    11. Below the existing `<SubscriptionDetailDialog>`, render:
        - `<PaymentRecordFormDialog open={paymentSubId !== null} onOpenChange={(open) => { if (!open) setPaymentSubId(null) }} subscriptionId={paymentSubId ?? undefined} />`
        - `<BatchRenewDialog open={batchOpen} onOpenChange={setBatchOpen} subscriptions={sorted.filter(s => selectedIds.has(s.id))} onSuccess={() => setSelectedIds(new Set())} />` (per D-13, clear selection after success).

    Do NOT remove the trash from `SubscriptionDetailDialog` or anywhere else — only the list-row trash is removed (D-05).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <done>tsc clean; lint clean; manual smoke: select 2 rows → 續訂 button appears → dialog lists 2 with date deltas; click wallet on a row → PaymentRecordFormDialog opens with subscriptionId set; trash icon no longer in row.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- `npm run lint` clean
- Manual: select-all checkbox toggles all rows; indeterminate state shows when partial; 續訂 button hidden with 0 selection; BatchRenewDialog flags cancelled/null-cycle rows in red and excludes them from POST body; on success, checkboxes clear and list refreshes
- Manual (D-02): open a subscription detail dialog → confirm PaymentRecordList section still renders with payment history and edit/delete buttons (unchanged)
</verification>

<success_criteria>
- D-01, D-05, D-06, D-07, D-08, D-10, D-11, D-13, D-14 all visibly satisfied in the UI
- No call to deleted endpoint from list row
- Batch-renew POST body contains only eligible ids
</success_criteria>

<output>
After completion, create `.planning/phases/04-ux-renewal/04-02-SUMMARY.md`
</output>
