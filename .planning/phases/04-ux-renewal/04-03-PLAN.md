---
phase: 04-ux-renewal
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/subscriptions/SubscriptionForm.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "billing_cycle is required in the frontend form (zod enum without optional); submitting blank shows 「請選擇計費週期」 error"
    - "Edit mode shows a 「刪除訂閱」 danger button at the bottom of the form; create mode does not show it"
    - "Clicking the delete button opens a confirmation dialog naming the service; confirming calls DELETE /api/v1/subscriptions/{id}, invalidates ['subscriptions'], toasts success, and navigates back to /subscriptions"
    - "Existing subscriptions with null billing_cycle force the user to choose one before saving on next edit"
  artifacts:
    - path: frontend/src/components/subscriptions/SubscriptionForm.tsx
      provides: "Required billing_cycle + bottom delete button (edit mode only)"
  key_links:
    - from: frontend/src/components/subscriptions/SubscriptionForm.tsx
      to: deleteSubscription
      via: "useMutation in form"
      pattern: "deleteSubscription"
---

<objective>
編輯頁加入「刪除訂閱」危險按鈕，並讓 billing_cycle 在前端表單變成必填。

Purpose: 實作 D-09 與 D-15。
Output: 更新 SubscriptionForm 元件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-ux-renewal/04-CONTEXT.md
@frontend/src/components/subscriptions/SubscriptionForm.tsx
@frontend/src/components/subscriptions/DeleteConfirmDialog.tsx
@frontend/src/api/subscriptions.ts

<interfaces>
From `frontend/src/api/subscriptions.ts`:
- `deleteSubscription(id: number): Promise<void>` — already exists.

From `frontend/src/components/subscriptions/SubscriptionForm.tsx`:
- Props include `defaultValues?: FormValues`. The form is used by both create and edit pages; create passes no `defaultValues`. We must distinguish modes — see action below.

From `frontend/src/components/ui/dialog`:
- `Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogDescription`, `DialogFooter` (same usage as in DeleteConfirmDialog.tsx).

From `react-router-dom`:
- `useNavigate()` (already imported in the form).

From `frontend/src/hooks/use-toast`:
- `useToast()` returns `{ toast }`.

Current schema uses `billing_cycle: z.enum(BILLING_CYCLES, { error: '請選擇計費週期' })` — already non-optional in the type. But `defaultValues` block (line ~153) does NOT set a default for `billing_cycle`, so an unselected create form will fail validation on submit, which is the desired behavior — confirm this still works. The real change for D-15 is to make sure no path silently falls back to a value (toFormValues currently falls back to 'monthly' for null; per D-15 we must NOT auto-pick — force the user to choose).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修正 toFormValues 避免 billing_cycle 自動帶 'monthly'</name>
  <files>frontend/src/components/subscriptions/SubscriptionForm.tsx</files>
  <read_first>
    - frontend/src/components/subscriptions/SubscriptionForm.tsx
  </read_first>
  <action>
    Per D-15, existing rows with `billing_cycle == null` must force the user to choose on edit, not silently default to 'monthly'.

    1. Change `FormValues` so `billing_cycle` accepts being initially empty: keep zod enum as required (`z.enum(BILLING_CYCLES, { error: '請選擇計費週期' })`) but allow the runtime default to be `undefined`. Approach: relax the TS type by treating `defaultValues.billing_cycle` as possibly undefined. Concretely, change `toFormValues` so `billing_cycle: sub.billing_cycle ?? 'monthly'` becomes `billing_cycle: sub.billing_cycle as FormValues['billing_cycle']` (allowing the runtime to pass through null/undefined). Cast accordingly.
    2. Update the `<Select defaultValue={defaultValues?.billing_cycle}>` to also pass `value={watch('billing_cycle')}` so React re-renders the placeholder when the field is unset.
    3. Ensure the SelectTrigger's `<SelectValue placeholder="請選擇" />` shows for null/unset.
    4. Confirm that submitting without selecting shows the existing error 「請選擇計費週期」 from zod.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>tsc clean; manual: editing a subscription whose backend record has null billing_cycle shows empty Select with placeholder 「請選擇」; submit fails with 「請選擇計費週期」.</done>
</task>

<task type="auto">
  <name>Task 2: 表單底部加入「刪除訂閱」危險按鈕（僅編輯模式）</name>
  <files>frontend/src/components/subscriptions/SubscriptionForm.tsx</files>
  <read_first>
    - frontend/src/components/subscriptions/SubscriptionForm.tsx (after Task 1)
    - frontend/src/components/subscriptions/DeleteConfirmDialog.tsx
  </read_first>
  <action>
    Add edit-mode delete capability per D-09.

    1. Extend `Props` to include optional `subscriptionId?: number` and optional `serviceName?: string`. When both present → render delete button. When absent → form is in create mode and delete UI is hidden.
    2. Update the two consuming pages to pass these props. Find them via the imports of `SubscriptionForm`:
       - The edit page (likely `frontend/src/pages/SubscriptionEditPage.tsx` or similar) must pass `subscriptionId={sub.id}` and `serviceName={sub.service_name}`.
       - The create page must NOT pass them.
       (Locate exact file via `grep -r "from '@/components/subscriptions/SubscriptionForm'" frontend/src` before editing.)
    3. Inside `SubscriptionForm`, add a `useState<boolean>` `deleteOpen`, a `useMutation` calling `deleteSubscription(subscriptionId)`, with `onSuccess`: `queryClient.invalidateQueries({ queryKey: ['subscriptions'] })`, `toast({ title: \`「${serviceName}」已刪除\` })`, `navigate('/subscriptions')`; `onError`: `toast({ title: '刪除失敗', variant: 'destructive' })`.
    4. Below the existing footer (取消/儲存) buttons, add a visually separated danger zone:
       ```
       {subscriptionId !== undefined && serviceName !== undefined && (
         <section className="mt-12 border-t border-destructive/30 pt-6">
           <h3 className="text-base font-semibold text-destructive">危險操作</h3>
           <p className="mt-1 text-sm text-muted-foreground">刪除後無法復原。</p>
           <Button type="button" variant="destructive" className="mt-3" onClick={() => setDeleteOpen(true)}>
             刪除訂閱
           </Button>
         </section>
       )}
       ```
    5. Render a `<Dialog open={deleteOpen} onOpenChange={setDeleteOpen}>` with title 「確認刪除」, description `確定要刪除「${serviceName}」嗎？此操作無法復原。`, footer 取消 + 確認刪除 (`variant="destructive"`, calls the mutation). Mirror the structure of `DeleteConfirmDialog.tsx`.
    6. Import `useQueryClient` from `@tanstack/react-query`, `deleteSubscription` from `@/api/subscriptions`, `useToast` from `@/hooks/use-toast`, and the dialog primitives.

    Do NOT remove the existing 取消 / 儲存 buttons.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <done>Edit page shows delete button; create page does not; clicking → confirm dialog → DELETE call → toast + navigate.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- `npm run lint` clean
- Manual: create page has no delete UI; edit page shows danger zone at bottom; null-billing-cycle row forces selection before save
</verification>

<success_criteria>
- D-09 satisfied: delete only in edit page, confirmation required
- D-15 satisfied: frontend rejects empty billing_cycle and surfaces clear error
</success_criteria>

<output>
After completion, create `.planning/phases/04-ux-renewal/04-03-SUMMARY.md`
</output>
