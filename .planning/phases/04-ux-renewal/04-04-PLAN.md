---
phase: 04-ux-renewal
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/PaymentRecordsPage.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Global PaymentRecordsPage has no 新增付款 button (creation only happens from subscription list wallet icon per D-04)"
    - "Each row in PaymentRecordsPage has an edit (Pencil) button (when canUpdate) that opens PaymentRecordFormDialog pre-filled with the record"
    - "Each row has a delete (Trash2) button (when canDelete) that opens a confirmation dialog; confirming calls deletePayment, invalidates ['payments'], and toasts success"
  artifacts:
    - path: frontend/src/pages/PaymentRecordsPage.tsx
      provides: "Edit/delete actions per row consistent with PaymentRecordList behavior"
  key_links:
    - from: frontend/src/pages/PaymentRecordsPage.tsx
      to: PaymentRecordFormDialog
      via: "edit click sets editing record and opens dialog"
      pattern: "PaymentRecordFormDialog"
---

<objective>
全域付款頁加入每筆紀錄的編輯與刪除操作，行為與 detail dialog 內的 PaymentRecordList 一致。

Purpose: 實作 D-03, D-04。
Output: 更新 PaymentRecordsPage。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-ux-renewal/04-CONTEXT.md
@frontend/src/pages/PaymentRecordsPage.tsx
@frontend/src/components/payments/PaymentRecordList.tsx
@frontend/src/components/payments/PaymentRecordFormDialog.tsx
@frontend/src/api/payment_records.ts

<interfaces>
The page ALREADY has most of the required wiring (read the file — edit/delete state, mutation, dialog rendering are present from a prior phase). Audit reveals it ALSO already has `PaymentRecordFormDialog` rendered. Confirm the following on read:
- `editing` state, `formOpen` state, `deletingId` state — present
- Edit click handler `setEditing(r); setFormOpen(true)` — present
- Delete click handler `setDeletingId(r.id)` — present
- delete mutation invalidating `['payments']` — present

If everything above is already wired correctly, this plan reduces to a verification + minor cleanup. The two things that MUST change per D-04:
1. The page MUST NOT show a 新增付款 button. Audit existing source — if a create button exists, remove it. (Current file does NOT show one, but verify.)
2. `PaymentRecordFormDialog` is rendered without `subscriptionId` — that is correct for edit-only usage, but make sure the form only opens with `record` set (edit mode). Add a guard so the dialog cannot be opened in create mode from this page.

Also: ensure `formOpen` is reset to false when `editing` becomes undefined to avoid stale-open bug.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 審計並補齊 PaymentRecordsPage 的編輯/刪除行為，移除新增付款入口</name>
  <files>frontend/src/pages/PaymentRecordsPage.tsx</files>
  <read_first>
    - frontend/src/pages/PaymentRecordsPage.tsx
    - frontend/src/components/payments/PaymentRecordList.tsx (reference behavior to match)
    - frontend/src/components/payments/PaymentRecordFormDialog.tsx
  </read_first>
  <action>
    Read PaymentRecordsPage.tsx fully. Then ensure D-03 and D-04 are satisfied:

    1. **D-04 — no create button**: Scan for any 新增付款 / `Plus` icon / button creating payments. If present, remove it and remove unused imports.
    2. **D-03 — edit action**: Confirm Pencil button exists per row inside the `canUpdate &&` gate; clicking sets `editing` and opens `PaymentRecordFormDialog`. If missing, add it.
    3. **D-03 — delete action**: Confirm Trash2 button exists per row inside the `canDelete &&` gate; clicking sets `deletingId`. Confirm a confirmation Dialog renders and the delete mutation invalidates `['payments']`. If any piece is missing, add it.
    4. **PaymentRecordFormDialog correctness**:
       - Pass `record={editing}` only (no `subscriptionId` since editing carries it).
       - Add a defensive: open the form ONLY when `editing !== undefined`. Change the open condition to `formOpen && editing !== undefined` OR add `useEffect(() => { if (!formOpen) setEditing(undefined) }, [formOpen])` to clean up state on close.
    5. **Empty state colSpan**: After any column changes, verify `colSpan` in the empty row matches the current header count.
    6. Verify imports are all used (remove `Plus` if no longer needed).

    Do not change the search/filter UI at the top of the page.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <done>Page has no 新增付款 button; edit pencil opens the dialog pre-filled; delete trash opens confirm dialog and removes the record; tsc + lint clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- `npm run lint` clean
- Manual: load /payments, search range, edit a record (dialog opens with values), save → list refreshes; delete a record → confirm → row gone
- Confirm no 新增付款 button visible anywhere on /payments
</verification>

<success_criteria>
- D-03 satisfied: edit and delete per row work identically to PaymentRecordList
- D-04 satisfied: no create entry point on the global page
</success_criteria>

<output>
After completion, create `.planning/phases/04-ux-renewal/04-04-SUMMARY.md`
</output>
