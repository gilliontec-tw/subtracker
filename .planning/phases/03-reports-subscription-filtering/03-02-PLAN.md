---
phase: 03-reports-subscription-filtering
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified: []
autonomous: true
requirements:
  - REPORT-02
  - SUBSCR-02

must_haves:
  truths:
    - "The reports route uses subscription.annual_cost() from the domain entity as the sole cost calculation — no inline duplicate"
    - "The subscription list page filters by department, category, and status via JS applyFilters() — all three filters work correctly"
    - "All 47+ pytest tests pass after Phase 3 Plan 01 changes"
  artifacts:
    - path: "src/interfaces/web/routes/subscriptions.py"
      provides: "annual_cost() called exclusively from domain entity (no inline lambda or function)"
      contains: "annual_cost()"
    - path: "src/interfaces/web/templates/index.html"
      provides: "three filter dropdowns wired to applyFilters() + data-attributes on tr rows matching filter values"
      contains: "applyFilters"
    - path: ".planning/phases/03-reports-subscription-filtering/03-02-SUMMARY.md"
      provides: "verification documentation for REPORT-02 and SUBSCR-02"
  key_links:
    - from: "subscriptions.py reports handler (annual_cost calls)"
      to: "src/domain/entities/subscription.py Subscription.annual_cost()"
      via: "s.annual_cost() method call"
      pattern: "s\\.annual_cost\\(\\)"
    - from: "index.html filter dropdowns"
      to: "index.html tr data-attributes"
      via: "applyFilters() reading dataset.status / dataset.cat / dataset.dept"
      pattern: "applyFilters|data-status|data-cat|data-dept"
---

<objective>
Verify REPORT-02 (annual_cost deduplication) and SUBSCR-02 (subscription filtering) are fully satisfied — no code changes expected. Document findings in the SUMMARY.

Purpose: Both requirements were implemented in prior phases. This plan performs explicit source-level verification, runs the full test suite, and records the evidence so checkers and the user can confirm pass without re-reading source files.

Output: SUMMARY.md with verification evidence for REPORT-02 and SUBSCR-02.
</objective>

<execution_context>
@C:/Users/Gillion-ADM-015/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gillion-ADM-015/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-reports-subscription-filtering/03-CONTEXT.md
@.planning/phases/03-reports-subscription-filtering/03-01-SUMMARY.md
@.planning/phases/03-reports-subscription-filtering/03-PATTERNS.md

<interfaces>
<!-- Key evidence locations. Executor reads these to confirm and document. -->

REPORT-02 evidence — subscriptions.py:
  Line 418: cur_cat_map[cur][k]["cost"] += s.annual_cost()
  Line 420: cur_totals[cur] += s.annual_cost()
  (After Plan 01: these lines are now inside _build_report_sections helper)
  Confirm: no function named "annual_cost" defined anywhere in subscriptions.py (other than the method call)
  Confirm: no lambda computing annual cost (e.g., lambda s: s.cost * 12) present in subscriptions.py

SUBSCR-02 evidence — index.html:
  statusFilter select id with onchange="applyFilters()" — values: active, renewed, cancelled, suspended
  catFilter select id with onchange="applyFilters()" — options derived from s.category
  deptFilter select id with onchange="applyFilters()" — options derived from s.department
  tr data-status="{{ s.status.value }}" — matches statusFilter option values
  tr data-cat="{{ s.category or '' }}" — matches catFilter
  tr data-dept="{{ s.department or '' }}" — matches deptFilter
  applyFilters() function: reads dataset.status, dataset.cat, dataset.dept for each tr

Domain entity:
  src/domain/entities/subscription.py — annual_cost() method (lines 52-62)
  This is the single source of truth. Reports route must call this method, not reimplement the logic.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify REPORT-02 — confirm annual_cost() is the sole cost calculation in reports route</name>
  <files></files>
  <read_first>
    - src/interfaces/web/routes/subscriptions.py (full file — scan for any inline annual cost calculation)
    - src/domain/entities/subscription.py (lines 52-62 — annual_cost method reference)
  </read_first>
  <action>
    Read subscriptions.py in full. Confirm:
      1. The string "annual_cost" appears at minimum twice in the file (two s.annual_cost() calls inside _build_report_sections, added in Plan 01).
      2. There is no function definition `def annual_cost` anywhere in subscriptions.py.
      3. There is no lambda that multiplies cost by a billing cycle multiplier (e.g., no `lambda s: s.cost * 12` or similar inline cost computation).
      4. The import list at the top of subscriptions.py does NOT import annual_cost as a standalone function (it is accessed as a method on the entity instance).

    Run the grep command to confirm absence of inline definition:
      grep -n "def annual_cost\|lambda.*cost.*12\|cost.*multiplier" src/interfaces/web/routes/subscriptions.py

    Expected output: no matches (empty).

    Record: which line numbers contain s.annual_cost() calls (should be at minimum lines inside _build_report_sections after Plan 01 refactor).

    Per D-05 (CONTEXT.md): the route must use subscription.annual_cost() from the domain entity. REPORT-02 passes if no inline duplicate exists.
  </action>
  <verify>
    <automated>python -c "import ast, sys; src=open('src/interfaces/web/routes/subscriptions.py').read(); tree=ast.parse(src); fns=[n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]; assert 'annual_cost' not in fns, f'Inline annual_cost function found: {fns}'; print('REPORT-02 PASS: no inline annual_cost in subscriptions.py')"</automated>
  </verify>
  <acceptance_criteria>
    - The Python AST check exits 0 and prints "REPORT-02 PASS: no inline annual_cost in subscriptions.py"
    - grep for "def annual_cost" in subscriptions.py returns zero matches
    - grep for "s.annual_cost()" in subscriptions.py returns at least 2 matches (inside _build_report_sections)
  </acceptance_criteria>
  <done>
    REPORT-02 confirmed: annual_cost() is defined once in src/domain/entities/subscription.py and called (not redefined) in subscriptions.py. No inline duplicate exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify SUBSCR-02 — confirm three filters are wired correctly in index.html, then run full pytest suite</name>
  <files></files>
  <read_first>
    - src/interfaces/web/templates/index.html (full file — read to confirm filter dropdowns, data-attributes, and applyFilters JS)
    - .planning/phases/03-reports-subscription-filtering/03-PATTERNS.md (lines 317-376 — exact filter verification checklist)
  </read_first>
  <action>
    Read index.html in full. Confirm the following four assertions are true:

    ASSERTION 1 — Three filter select elements exist with correct IDs and onchange handlers:
      - id="statusFilter" with onchange="applyFilters()" — option values: "", "active", "renewed", "cancelled", "suspended"
      - id="catFilter" with onchange="applyFilters()" — options populated from subscriptions|map(attribute='category')|select|unique
      - id="deptFilter" with onchange="applyFilters()" — options populated from subscriptions|map(attribute='department')|select|unique

    ASSERTION 2 — Table row data-attributes align with filter values:
      - data-status="{{ s.status.value }}" — the .value of SubscriptionStatus enum produces "active"/"renewed"/"cancelled"/"suspended", matching statusFilter option values exactly
      - data-cat="{{ s.category or '' }}" — empty string when no category, matching catFilter "all" case
      - data-dept="{{ s.department or '' }}" — empty string when no department, matching deptFilter "all" case

    ASSERTION 3 — applyFilters() function reads the correct dataset properties:
      - tr.dataset.status compared to statusFilter value
      - tr.dataset.cat compared to catFilter value
      - tr.dataset.dept compared to deptFilter value
      - All four conditions (q, st, cat, dept) use AND logic with empty-string as "show all" sentinel

    ASSERTION 4 — rowCount element updated after each filter to reflect visible row count.

    After confirming all four assertions, run the full pytest suite.

    Per D-06 (CONTEXT.md): SUBSCR-02 is already implemented. Document as passed in the SUMMARY. No code changes.
  </action>
  <verify>
    <automated>pytest --tb=short -q</automated>
  </verify>
  <acceptance_criteria>
    - pytest exits 0 with all 47+ tests passing (including the 4 new tests from Plan 01)
    - grep -c "applyFilters" src/interfaces/web/templates/index.html returns 4 or more (3 onchange= calls + the function definition)
    - grep -c "data-status\|data-cat\|data-dept" src/interfaces/web/templates/index.html returns 3 or more
    - All four SUBSCR-02 assertions confirmed as true by reading index.html source
  </acceptance_criteria>
  <done>
    SUBSCR-02 confirmed: department, category, and status filters are wired correctly with matching data-attributes and applyFilters() logic. All 47+ pytest tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundaries | This plan adds no new code paths — verification only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | index.html filter dropdowns expose all unique category and department values from subscription data | accept | Data is visible to any authenticated user on the main list page already; no new exposure introduced |
| T-03-06 | Tampering | Client-side JS filters (applyFilters) can be bypassed by DOM manipulation | accept | Filters are display-only (CSS show/hide); all data is already loaded in the page for authorized users; no server-side data access controlled by these filters |
</threat_model>

<verification>
Full test suite: `pytest --tb=short -q` — all 47+ tests pass.

REPORT-02 source check:
  `python -c "import ast; src=open('src/interfaces/web/routes/subscriptions.py').read(); tree=ast.parse(src); fns=[n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]; assert 'annual_cost' not in fns; print('PASS')"`

SUBSCR-02 source check:
  `grep -c "applyFilters" src/interfaces/web/templates/index.html` — expect 4+
  `grep -c "data-status\|data-cat\|data-dept" src/interfaces/web/templates/index.html` — expect 3+
</verification>

<success_criteria>
- REPORT-02: AST check confirms no def annual_cost in subscriptions.py; s.annual_cost() calls confirmed present
- SUBSCR-02: All four filter assertions confirmed true from index.html source; no code changes needed
- pytest exits 0 with 47+ tests passing
- SUMMARY documents pass evidence for both requirements
</success_criteria>

<output>
After completion, create `.planning/phases/03-reports-subscription-filtering/03-02-SUMMARY.md`
</output>
